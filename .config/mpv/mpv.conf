# Use the OpenGL rendering path. The default Vulkan path (gpu-next/libplacebo)
# runs out of device memory on 4K video (VK_ERROR_OUT_OF_DEVICE_MEMORY -> black
# screen, audio only) on this Nvidia setup.
gpu-api=opengl

# Hardware decoding (nvdec on Nvidia).
hwdec=auto-safe
